#!/usr/bin/make -f

%:
	dh $@ --with=python3

execute_after_dh_install:
	mkdir -p debian/store-provider/usr/share/store-provider/
	mv debian/store-provider/usr/share/dbus-1/services/io.FuriOS.AndroidStore.service debian/store-provider/usr/share/store-provider/io.FuriOS.AndroidStore.service
